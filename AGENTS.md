# AGENTS.md

Read [`CLAUDE.md`](CLAUDE.md) — it is this repo's entry point and invariants digest, with a
routing table into `docs/` (the invariant source; read the relevant doc before touching a
subsystem). Everything there is tool-agnostic despite the filename.

Two rules worth repeating before you even open it: **push to `main` = live deploy** to GitHub
Pages, and this repo is **public** — nothing personal, no secrets beyond the deliberately-public
Supabase publishable key in `src/config.ts`.
